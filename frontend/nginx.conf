server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Proxy inverso para redirigir peticiones de API al Backend
    # Se usa una variable para evitar que Nginx se caiga al iniciar si el host "backend" no es resoluble por DNS
    location /api/ {
        resolver 127.0.0.11 8.8.8.8 valid=30s;
        set $backend_host "backend";
        proxy_pass http://$backend_host:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Endpoint de Health Check para el Frontend (ALB / ECS Target Group)
    location /health {
        access_log off;
        default_type text/plain;
        return 200 "healthy\n";
    }

    # Páginas de error
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
